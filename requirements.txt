# Requirements for Radio Transcription Tool v3.7 (Modular Structure)
# Core dependencies
openai>=1.0.0
pydub>=0.25.1
# tkinter is part of Python's standard library - no need to install

# Optional dependencies for enhanced functionality
keybert>=0.8.0
sentence-transformers>=2.2.0
transformers>=4.20.0
torch>=1.12.0
tiktoken>=0.5.0  # Required for KeyBERT compatibility

# Audio processing
numpy>=1.21.0
scipy>=1.8.0

# Utilities
requests>=2.28.0
urllib3>=1.26.0

# Development dependencies (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=5.0.0
